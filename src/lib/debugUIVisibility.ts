/**
 * DEBUG UI VISIBILITY
 *
 * Set to `true` to hide all debug UI overlays (badges, toggles, trace controls).
 * Underlying instrumentation and logging are UNAFFECTED.
 */
export const HIDE_DEBUG_UI = true;